Drop your overlay PNGs here:
  uncommon.png  — applied to all uncommon cards at 22% opacity
  rare.png      — applied to all rare and secret rare cards at 22% opacity (SR at 28%)

Recommended: use a foil/shimmer/sparkle texture PNG with transparent background.
The overlay uses mix-blend-mode: screen so light areas will show through and dark areas won't.
